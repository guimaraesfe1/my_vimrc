set nocompatible 				" Habilita compatibilldade com o VIM e nao com o VI.
set number 						" Habilita os números das linhas.
set relativenumber 				" Habilita os números de linhas relativas a posição do cursor.
set mouse=a 					" Habilita o mouse.
set background=dark 			" Configura tema de fundo.
set tabstop=4 					" Configura o número de espaços da tabulação.
set history=100					" Configura o número máximo de comandos registrados no historico.
set showmode					" Mostra o modo atual do VIM no canto inferior esquerdo.
set showcmd						" Mostra o comando que esta sendo digitado no canto inferior direito.
set cursorline					" Mostra uma 'linha guia horizontal' na linha do cursor.
set cursorcolumn				" Mostra um 'linha guia vertical' na coluna do cursor.
set ruler 						" Mostra a posição do cursor no canto inferior direito.
set colorcolumn=110				" Mostra uma 'linha guia vertical' fixa no número armazenado na opção.
set backspace=indent,eol,start 	" Sobrescreve indentação, quebra de linhas e começo de inserções.
set autoindent 					" Copia a indentação da linha atual quando começa uma nova linha.
set nolist						" Não mostra '$' e '^I' no lugar de quebra de linhas e espaços.

syntax on 						" Habilita o destaque na sintaxe.
filetype on						" Habilita a detecção automática do tipo do arquivo.
